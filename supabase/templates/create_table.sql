-- ملف إنشاء الجداول

-- =====================================================
-- Marketna E-Commerce - Empty Schema Template
-- File: empty.sql
-- Version: 1.0
-- Date: 2026-03-21
-- Description: قالب فارغ لجدول جديد
-- Dependencies: None
-- =====================================================

-- =====================================================
-- 📋 محتويات الملف
-- =====================================================
-- 1. تنظيف كامل قبل الإنشاء
-- 2. إنشاء الجدول
-- 3. الفهارس (Indexes)
-- =====================================================


-- =====================================================
-- 1️⃣ تنظيف كامل قبل الإنشاء
-- =====================================================

-- DROP INDEX IF EXISTS idx_your_table_column;
-- DROP TABLE IF EXISTS public.your_table CASCADE;


-- =====================================================
-- 2️⃣ إنشاء الجدول
-- =====================================================

-- CREATE TABLE public.your_table (
--   id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
--   created_at TIMESTAMPTZ DEFAULT NOW()
-- );

-- Comments
-- COMMENT ON TABLE public.your_table IS 'وصف الجدول';
-- COMMENT ON COLUMN public.your_table.id IS 'المعرف الفريد';


-- =====================================================
-- 3️⃣ الفهارس (Indexes)
-- =====================================================

-- CREATE INDEX idx_your_table_column ON public.your_table(column);

-- COMMENT ON INDEX idx_your_table_column IS 'وصف الفهرس';


-- =====================================================
-- ✅ نهاية الملف
-- =====================================================
