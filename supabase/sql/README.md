-- =====================================================
-- Marketna E-Commerce - Schema Index
-- فهرس ملفات قاعدة البيانات
-- =====================================================

-- ترتيب تشغيل الملفات:
-- 1. 00-enums.sql          - القوائم المنسدلة (Types)
-- 2. 01-profiles.sql       - جدول الملفات الشخصية
-- 3. 02-triggers.sql       - الدوال التلقائية والمشغلات
-- 4. 03-rls-policies.sql   - سياسات الأمان

-- =====================================================
-- كيفية التشغيل:
-- =====================================================

-- الطريقة 1: تشغيل كل ملف على حدة (موصى به)
--   1. افتح Supabase Dashboard > SQL Editor
--   2. انسخ محتوى كل ملف
--   3. الصق وشغّل بالترتيب

-- الطريقة 2: تشغيل جميع الملفات دفعة واحدة
--   cat sql/*.sql | psql -h db.xxx.supabase.co -U postgres -d postgres

-- الطريقة 3: استخدام Supabase CLI
--   supabase db reset --linked

-- =====================================================
-- هيكل قاعدة البيانات:
-- =====================================================

-- الجداول:
--   - profiles: الملفات الشخصية للمستخدمين

-- الأنواع (Enums):
--   - gender: نوع الجنس

-- الدوال (Functions):
--   - create_profile_for_new_user(): إنشاء ملف عند التسجيل
--   - update_last_login(): تحديث آخر دخول

-- المشغلات (Triggers):
--   - on_auth_user_created: إنشاء ملف عند تسجيل مستخدم جديد

-- السياسات (RLS Policies):
--   - profiles_public_read: عرض عام للملفات
--   - profiles_user_update_own: تحديث الملف الشخصي
--   - profiles_user_insert_own: إنشاء ملف شخصي

-- =====================================================
-- ملاحظات:
-- =====================================================

-- 1. يتم إنشاء جدول profiles تلقائياً عند التسجيل
-- 2. البريد الإلكتروني لا يمكن تحديثه (مثبت من auth.users)
-- 3. last_login_at يُحدث تلقائياً عند تسجيل الدخول
-- 4. RLS يحمي البيانات ويسمح بالوصول المصرح به فقط

-- =====================================================
